For real time face mask detection we can pass the each fram in the model and get the result.
Here we have accessed the web cam using the colab , so we could pass only one image in one go, with same prediction.
